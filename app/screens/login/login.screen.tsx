import React from 'react';
import { SafeAreaView } from 'react-native';
import { Card } from 'react-native-paper';

export const LoginScreen = () => {

    return(
        <SafeAreaView>
            <Card>
                <Card.Title title="Social Reading App with Native"></Card.Title>
            </Card>
        </SafeAreaView>
    );

}